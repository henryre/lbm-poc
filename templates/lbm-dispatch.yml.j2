name: "LBM: Issue Dispatch"
on:
  issues:
    types: [labeled, closed]
permissions:
  contents: write
  issues: write
  actions: write
  pull-requests: write
  id-token: write
jobs:
  dispatch:
    uses: {{ lbm_repo }}/.github/workflows/_dispatch.yml@{{ lbm_ref }}
    secrets: inherit
    with:
      config-path: lbm.toml
{% if 'claude' in harnesses %}
  claude:
    if: github.event.action == 'labeled'
    uses: {{ lbm_repo }}/.github/workflows/_agent-claude.yml@{{ lbm_ref }}
    secrets: inherit
    with:
      config-path: lbm.toml
{% endif %}
